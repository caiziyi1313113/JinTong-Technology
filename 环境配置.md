# 1）系统依赖（照旧）

```
sudo apt update
sudo apt install -y postgresql postgresql-contrib nodejs npm libpq-dev gcc

```

> 我把 `libpq-dev gcc` 也提前装上，避免 `psycopg2` 编译报错。

---

# 2）PostgreSQL（照旧）

```
sudo service postgresql start
sudo -u postgres psql -c "CREATE DATABASE msa;"
sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD 'msa';"

```

（如果提示 database 已存在，就不用管）

### 1. sudo service postgresql start

- **sudo**: 以超级用户权限执行。
    
- **service**: 这是一个用于管理系统服务的命令。
    
- **postgresql**: 指定要操作的服务名称，这里是 PostgreSQL 数据库服务。
    
- **start**: 启动该服务。
    

**含义**：这条命令的作用是**启动 PostgreSQL 数据库服务器**。当 PostgreSQL 软件包安装完成后，它通常不会自动立即运行，或者在系统重启后需要手动启动。这条命令确保数据库服务正在后台运行，以便可以进行连接和操作。

### 2. sudo -u postgres psql -c "CREATE DATABASE msa;"

- **sudo -u postgres**:
    
    - sudo: 以超级用户权限执行。
        
    - -u postgres: 指定以 postgres 这个用户身份来执行后续命令。在 PostgreSQL 安装后，系统会自动创建一个名为 postgres 的用户，它是 PostgreSQL 数据库的超级管理员账户（与系统用户 root 类似，但只针对数据库）。为了安全和权限管理，操作数据库通常会切换到 postgres 用户。
        
- **psql**: 这是 PostgreSQL 的命令行客户端工具。它可以用来连接到 PostgreSQL 数据库，并执行 SQL 命令。
    
- **-c "CREATE DATABASE msa;"**:
    
    - -c: 表示“执行一个命令”。
        
    - "CREATE DATABASE msa;": 这是 SQL 命令，用于**创建一个名为 msa 的新数据库**。
        

**含义**：这条命令的作用是**在 PostgreSQL 数据库中创建一个名为 stockai 的新数据库**。它通过 psql 客户端工具，以 postgres 数据库超级用户的身份执行 CREATE DATABASE SQL 命令。

### 3. sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD 'postgres';"

- **sudo -u postgres psql -c**: 与上一条命令相同，以 postgres 用户的身份执行 SQL 命令。
    
- **"ALTER USER postgres WITH PASSWORD 'msa';"**: 这是 SQL 命令，用于**修改名为 postgres 的用户的密码，将其设置为 'msa'**。
    

**含义**：这条命令的作用是**设置（或修改）PostgreSQL 数据库的超级管理员用户 postgres 的密码为 'postgres'**。默认情况下，postgres 用户可能没有密码，或者有一个随机生成的密码。为了方便管理和通过远程工具连接，通常会为其设置一个明确的密码。

---

# 3）Backend 用 conda 配置

## A. 创建并进入 conda 环境（替代 venv）

先确保 conda 可用：

`conda --version`

然后在项目根目录或 backend 目录都行，我建议在项目根目录执行：

```
conda create -n msa python=3.11 -y
conda activate msa

```

> 后续每次开发前，记得 `conda activate msa

---

## B. 安装后端依赖

进入 backend：

```
`cd backend`
```

安装 requirements：

```
`pip install -r requirements.txt`
```

### ✅ psycopg2 建议用二选一（更稳）

很多人卡在 psycopg2 编译。你可以这样处理：

**方案 1（最省事）：用二进制包**

```
pip install psycopg2-binary
```

**方案 2（更“生产”）：用 psycopg2 正式包**  
（你已经装了 `libpq-dev gcc`，一般就能过）

`pip install psycopg2`

> 如果 requirements.txt 里写死了 `psycopg2`，但你想用 binary，也没问题：先装 requirements，再 `pip install -U psycopg2-binary` 覆盖即可。

---

## C. 配置环境变量（和你之前一样）

```
export DATABASE_URL='postgresql+psycopg2://postgres:msa@localhost:5432/msa'
export JWT_SECRET='change-me'
export ALLOWED_ORIGINS='http://localhost:5173'

```

### 让它永久生效（推荐写到 ~/.bashrc，但注意 conda 初始化顺序）

```
echo "export DATABASE_URL='postgresql+psycopg2://postgres:msa@localhost:5432/msa'" >> ~/.bashrc
echo "export JWT_SECRET='change-me'" >> ~/.bashrc
echo "export ALLOWED_ORIGINS='http://localhost:5173'" >> ~/.bashrc
source ~/.bashrc

```

（如果你用 zsh 就写到 `~/.zshrc`）

---

## D. 初始化 DB + 启动后端

```
python scripts/init_db.py
python scripts/seed_demo.py
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

```

浏览器打开：

- Swagger: `http://localhost:8000/docs`
    
### 连接DBeaver
所以在 **DBeaver 里连接时这样填就可以：**

Host: localhost  
Port: 5432  
Database: postgres  
User: postgres  
Password: 你的密码
---

# 4）Frontend（照旧）
wsl中
新开一个终端（前端不需要 conda）：

```
cd frontend
npm install
npm run dev

```

打开：

- `http://localhost:5173`
    

---

# conda 常见坑（你可能会遇到）

## 1）终端里 python 不是 conda 的

检查：

```
which python
python --version

```

如果不是 conda 环境下的 python，说明你没 `conda activate stockai`。

## 2）psycopg2 报编译/链接错误

直接用：

`pip install -U psycopg2-binary`

一般立刻解决。

## 3）环境变量只在一个终端里生效

你在 A 终端 export 了，B 终端不会自动有。  
解决：写进 `~/.bashrc` 或者用 `.env`（如果你的 FastAPI 项目支持读取 .env）。

## 环境搭建
**你现在可以直接做**

1. 安装依赖并初始化库：按 README.md 执行 pip install -r backend/requirements.txt、python backend/scripts/init_db.py。
2. 跑每日流程：python backend/scripts/run_pipeline.py --mode all --date 2026-03-09 --top-n 30 --snapshot-type post_close。
3. 前端登录后进入 /discover 看排行榜和红绿冲突信号，/query 看五专家+投资专家详情，/track 看交易流水与计划信号。
4. 验证质朴API可以用
```
	export ZHIPU_API_KEY="你的新key"
	export ZHIPU_MODEL="glm-4.7-flash"
	
	python scripts/check_zhipu.py
```

- daily = 高频日常数据（行情/新闻/公告等）
- static = 低频基础资料（公司与财务全量）

**你现在可以这样用**

1. 初始化一次基础数据（用于后续“是否更新”检测）

`python backend/scripts/init_db.py python backend/scripts/run_pipeline.py --mode daily --date 2026-03-10`

2. 手动跑深圳主板A股全量 static（低频资料）

`python backend/scripts/run_static_all_sz_main.py --refresh-universe --batch-size 50 --sleep-seconds 0.8`

3. 前端用户查询单股时（/api/v1/analysis）会自动：
    - 检测并增量更新：行情/K线/公告/公司新闻/宏观
    - 再跑五专家 + 投资专家，返回最新结果